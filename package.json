{
  "name": "nextjs-kickstarter-installer",
  "private": true,
  "packageManager": "yarn@4.10.3",
  "scripts": {
    "install": "yarn install && tsx _installer/install.ts",
    "setup": "yarn install && tsx _installer/install.ts"
  },
  "devDependencies": {
    "inquirer": "^10.1.2",
    "tsx": "^4.16.2",
    "@types/inquirer": "^9.0.7"
  }
}
