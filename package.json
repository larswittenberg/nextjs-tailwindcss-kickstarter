{
  "name": "nextjs-kickstarter-installer",
  "private": true,
  "packageManager": "yarn@4.12.0",
  "scripts": {
    "setup": "yarn install --silent && yarn tsx _installer/install.ts",
    "setup:ci": "yarn install --silent && yarn tsx _installer/install.ts --yes"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.9",
    "inquirer": "^10.1.2",
    "tsx": "^4.21.0"
  }
}
